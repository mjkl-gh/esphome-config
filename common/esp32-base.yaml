esp32:
  framework:
    type: esp-idf

packages:
  wifi: !include wifi-with-captive-portal.yaml
  base: !include base.yaml

esp32_ble_tracker:
  scan_parameters:
    active: true

bluetooth_proxy:
  active: true